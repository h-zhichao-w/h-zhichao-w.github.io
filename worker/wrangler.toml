name = "visitor-map"
main = "worker.js"
compatibility_date = "2026-08-01"

# KV 命名空间：先执行 `wrangler kv namespace create VISITS`
# 然后把下面的 id 换成命令输出的 id
[[kv_namespaces]]
binding = "VISITS"
id = "REPLACE_WITH_KV_NAMESPACE_ID"

[vars]
# SALT 建议用 secret 管理：wrangler secret put SALT
